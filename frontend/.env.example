# Copy this file to frontend/.env for local testing,
# or set the same variable in your frontend hosting provider.
# Use your real backend origin without a trailing slash.

VITE_BACKEND_URL=http://localhost:5000